import React from 'react'; import { Link } from 'react-router-dom'; import { useCart } from '../context/CartContext'; import type { Product } from '../context/CartContext'; import { Plus } from 'lucide-react'; const ProductCard = ({ product }) => { const { addToCart } = useCart(); const primaryImage = product.images?.[0] || product.image; return ( <div className='product-card'> <Link to={`/product/${product.id}`}> <img src={primaryImage} alt={product.name} className='product-image' /> </Link> <div className='product-info'> <span className='product-category'>{product.category}</span> <Link to={`/product/${product.id}`} style={{ textDecoration: 'none' }}> <h3 className='product-title'>{product.name}</h3> </Link> <p className='product-price'>TSh {product.price.toLocaleString()}</p> <button className='add-to-cart-btn' onClick={() => addToCart(product)}> <Plus size={18} /> Add to Cart </button> </div> </div> ); }; export default ProductCard;
