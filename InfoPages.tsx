import React, { useState, useEffect } from 'react'; import { Mail, Phone, MapPin, ShieldCheck, FileText, RefreshCw, XCircle, Info, Send, Watch } from 'lucide-react'; const PageWrapper = ({ title, icon, children }) => ( <div className='container static-page-enhanced'> <h1>{title}</h1> {children} </div> ); export const About = () => { const [data, setData] = useState({ title: '', content: '' }); useEffect(() => { fetch('http://localhost:5000/api/pages/about').then(res => res.json()).then(setData); }, []); return ( <PageWrapper title={data.title}> <div>{data.content}</div> </PageWrapper> ); }; export const Contact = () => { return <div>Contact</div>; }; export const Policy = () => { return <div>Policy</div>; };

